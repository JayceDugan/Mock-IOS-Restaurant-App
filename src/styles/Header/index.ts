import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  base: {
    paddingTop: 70,
    fontSize: 30,
    textAlign: 'center',
    color: '#0066CC',
    fontWeight: '300',
    marginBottom: 20
  }
})
